<?xml version="1.0" encoding="utf-8"?>
<androidx.appcompat.widget.Toolbar
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        android:theme="@style/ThemeOverlay.AppCompat.ActionBar"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
        android:elevation="@dimen/elevacion_action_bar"
        >

        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@mipmap/huella"
            android:layout_gravity="start"
            android:id="@+id/imagenHuella"
            />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/Toolbar_name"
            android:layout_gravity="center"
            android:id="@+id/toolbar_title"
            android:textSize="@dimen/dimen_text"
            android:textColor="@color/colorSecondaryText"
            />

        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end"
            android:layout_marginEnd="@dimen/dimen_barMarginL"
            android:src="@mipmap/menu"
            />

        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end"
            android:layout_marginEnd="@dimen/dimen_barMarginR"
            android:src="@mipmap/star"
            android:id="@+id/imagenStar"
            android:onClick="irDetalle"
            />
</androidx.appcompat.widget.Toolbar>